# Sample project

Sample files for the tools demo -- a stand-in for whatever real project
opencode would be pointed at (`instance.directory` in the real source).

TODO: greet() should take a `greeting` argument instead of hardcoding "Hello".
